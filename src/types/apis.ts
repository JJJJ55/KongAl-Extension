export type NotificationItem = {
  title: string
  msg: string
}

export type SendMessageProps = {
  success: boolean
  data: any
}

export type LMSWebInfoProps = {
  success: boolean
  lmsUser: string
  xToken: string
}
